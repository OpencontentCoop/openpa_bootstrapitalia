<blockquote class="blockquote{if and(is_set( $align ), $align )} text-{$align}{/if}">
	<p class="mb-0">{$content}</p>
    {if is_set( $author )}<footer class="blockquote-footer">{$author}</footer>{/if}
</blockquote>
